version: "3"
services:
  web:
    build: .
    volumes:
      - "./app:/src/app"
      - "./assets:/src/assets"
      - "../personal-blog-database:/src/database"
      - "./views:/src/views"
      - "./app.js:/src/app.js"
    entrypoint: npm run serve:dev
    ports:
      - "43000:3000"
