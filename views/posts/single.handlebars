<div class="post" id="post-{{post._id}}">
  <h1>{{post.title}}</h1>

  {{> type type=post.type }}

  {{> tags tags=post.tags }}

  {{> share post=post }}

  <div>{{{ post.content }}}</div>

  <div><a href="/posts/{{post._id}}/edit"><i class="icon-note icons"></i> Edit</a></div>
</div>